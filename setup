#!/bin/bash

if [[ $shell =~ "zsh" ]]; then
	if [[ -z $status ]]; then
		pwd | xargs -I {} echo "PATH=$PATH:{}" >> ~/.zshrc && source ~/.zshrc
	fi
else
	if [[ -z $status ]]; then
		pwd | xargs -I {} echo "PATH=$PATH:{}" >> ~/.bashrc && source ~/.bashrc
	fi
fi